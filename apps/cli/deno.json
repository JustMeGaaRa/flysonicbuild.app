{
  "tasks": {
    "dev": "deno run --watch main.ts",
    "compile": "deno compile --name drone-cli --allow-read --allow-write --allow-env mod.ts",
    "install": "deno install --name drone-cli --allow-read --allow-write --allow-env mod.ts"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
